.vscode/settings.json

# Compiled binary — built with `go build` / `go install`, never committed.
/lyx
lyx.exe

# Machine-local muxpoc session state (per-cwd), never committed.
.lyx/

# === mill-managed (regenerated by mill-setup) ===
**/.millhouse/
**/.scratch/
**/.wiki/
**/.active/
**/_mill/*.active
**/.portals/
# === end mill-managed ===

# === lyx-managed ===
.lyx/
# === end lyx-managed ===
